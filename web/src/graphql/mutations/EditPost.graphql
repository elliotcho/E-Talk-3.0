mutation EditPost($newContent: String!, $postId: Int!) {
    editPost(newContent: $newContent, postId: $postId) {
        id
        content
        createdAt
        user {
            id
            firstName
            lastName
        }
    }
}