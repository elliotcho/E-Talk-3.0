query Notifications($cursor: String, $limit: Int!) {
    notifications(cursor: $cursor, limit: $limit) {
        hasMore
        notifications {
            text
            type
            createdAt
            receiverId
            senderId
            postId
            user {
                id 
                firstName
                lastName
                profileURL
            }
        }
    }
}